import React from "react";
import EndYear from "./end-year";

export default function index({ start, limit, height, width }) {
  return <EndYear start={start} limit={limit} height={height} width={width} />;
}
